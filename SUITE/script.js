document.getElementById("contactForm").addEventListener("submit", function (e) {
    e.preventDefault();
    // Vous pouvez ajouter ici le code pour envoyer le formulaire de contact, par exemple, via une API ou un service de messagerie.
    alert("Votre message a été envoyé avec succès !");
});
